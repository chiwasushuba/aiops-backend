# Copy this file to .env and replace the value with a locally generated secret.
POSTGRES_PASSWORD=replace-with-a-long-random-local-password

# Optional: change these if they conflict with an existing local database.
# POSTGRES_DB=aiops
# POSTGRES_USER=aiops
